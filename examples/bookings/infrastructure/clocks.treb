module bookings.infrastructure.clocks

// Adapters satisfying the Clock shape.

use bookings.application.ports

service SystemClock
  fn now() -> Instant
    sys.clock()

service FixedClock(at: Instant)
  fn now() -> Instant
    at
